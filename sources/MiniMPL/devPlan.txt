重构fromToString_detail.hpp
使用TFromTo，区分从字符串构造和类的字符串操作符等自动转换操作，特别是从字符串构造。
类的字符串操作符可以条件编译等，然后使用模板wrapper

feature:
1.TypeListDat to replace ParamPackage

5. makeCallbackWorker and UnaryFunction should resoulve parameter variant.
e.g. if function signature expects T&, CallParamPackage should accept T*,stlSmartptr<T>,iterator data